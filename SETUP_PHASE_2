#finish removing systemd
#cannot do this now
#sudo apt-get autoremove systemd

#setup kde
#no longer using kscreen
#rm -r ~/.local/share/kscreen
#cp -r kscreen ~/.local/share/kscreen/
cp -r applications /home/pi/.local/share/applications
cp powermanagementprofilesrc /home/pi/.config
cp plasma-org.kde.plasma.desktop-appletsrc /home/pi/.config
cp kwinrulesrc /home/pi/.config
cp kwinrc /home/pi/.config
cp ksmserverrc /home/pi/.config
cp kscreenlockerrc /home/pi/.config
cp breezerc /home/pi/.config
mkdir /home/pi/.config/menus
cp applications-kmenuedit.menu /home/pi/.config/menus/

sudo chown -R pi:pi /home/pi

#setup on screen keyboard
sudo cp keyboard.xml.STANDARD /usr/share/matchbox-keyboard/keyboard.xml

echo "All finished.  Reboot or login/logout once more and test the main thread with a USB keyboard attached.  If everything works, you can set it to autostart."

